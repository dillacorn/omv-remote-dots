# Docker's embedded DNS resolver.
# Keeping backend names in variables makes Nginx resolve them at request time,
# so one stopped optional application does not prevent nginx-pihole from starting.
resolver 127.0.0.11 ipv6=off valid=30s;
resolver_timeout 5s;
